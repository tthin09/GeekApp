export interface Album {
  id: string;
  title: string;
  userId: string;
  username: string;
}

export interface User {
  id: string;
  name: string;
  email: string;
  phone: string;
  website: string;
}